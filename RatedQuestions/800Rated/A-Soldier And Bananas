#include<stdio.h>
int main()
{
    int k,n,w;
    int total=0;
    scanf("%d %d %d",&k,&n,&w);
    for(int i =1;i<=w;i++)
    {
        total=total+i*k;
    }
    int needed;
    if (total>n)
    needed=total-n;
    else
    needed=0;
    
    printf("%d",needed);
}
